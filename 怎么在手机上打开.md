# 怎么在 iPhone 上把「我的记录」当 App 打开

你有两条路，按你的情况选。

---

## 方式一：家里 WiFi 直接开（最快，无需任何账号）

**前提**：电脑开着、电脑和 iPhone 连同一个 WiFi。

1. 双击项目里的 **`启动APP.bat`**（会弹出一个黑色窗口，别关它）。
   - 或手动：在这个文件夹里运行 `npm run preview -- --host --port 4173`
2. iPhone 用 **Safari** 打开： **http://<终端里 Network 那一行的地址>:4173**
   - 打不开就换终端里列出的另一个 Network 地址
   - （这两个是你电脑当前的局域网地址，换了 WiFi 或重启路由可能会变；变了就看黑窗口里 `Network:` 后面那一行。）
3. Safari 底部点 **分享按钮** → **添加到主屏幕** → 完成。
4. 主屏幕就出现「记录」图标，点开就是全屏 App。

> 这种方式：能记、能搜、能加图片附件，数据存在手机本地。
> **缺点**：① 电脑一关／睡眠就打不开了；② 因为不是 HTTPS，**自动定位**和**离线打开**用不了。

---

## 方式二：传到网上，随时随地都能开（推荐，5 分钟，免费、不强制注册）

这样会得到一个固定的 `https://xxx.netlify.app` 网址，**不用开电脑、不用同 WiFi**，自动定位和离线也都能用。

1. 电脑上先生成正式版本（已经生成好了，在 `dist` 文件夹；如改过代码就重跑 `npm run build`）。
2. 浏览器打开 **https://app.netlify.com/drop**
3. 把项目里的 **`dist` 整个文件夹** 拖进那个网页。
4. 等几秒，它给你一个网址，比如 `https://random-name.netlify.app`。
5. iPhone Safari 打开这个网址 → 分享 → 添加到主屏幕。搞定。

> 想让这个网址永久保留 / 以后能更新，可以免费注册一下 Netlify（用邮箱或 GitHub）。不注册的话链接也能用一阵子。
> 数据依然只存在你自己手机里，不会上传到任何服务器。

---

## 我现在帮你做到哪了
- 网页四件套（时间线 / 写入 / 搜索 / 图片）+ 日历总览 + 目标 / 置顶，全做完并测过。
- PWA 已配好（图标、全屏、离线缓存都就绪）。
- 已在你电脑上把方式一的服务器**起好了**（只要电脑没关、没睡，现在就能用方式一打开）。

有问题等你回来我接着调。
